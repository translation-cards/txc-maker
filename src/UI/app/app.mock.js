angular.module('txcmaker', [])

// .run(function($httpBackend) {
//   //have to do this (and include ngMockE2E) when angularCSS is a dep of the app
//   $httpBackend.whenGET('views/DeckImport/DeckImport.html').passThrough();
// });
